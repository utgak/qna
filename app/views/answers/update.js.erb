$('.answer-errors').html('<%= render 'shared/errors', resource: @answer %>');

<% unless @answer.errors.present? %>
  $(".answers").find("[data-id=<%= @answer.id %>]").replaceWith('<%= j render @answer %>')
  $('form#edit-answer-' + <%= @answer.id %>).hide();
<% end %>
